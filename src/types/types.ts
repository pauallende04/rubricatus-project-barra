export interface MenuItem{
    title: string;
    url: string;
}
export interface BebidasInv{
    nombre: string;
    estado: string;
    precio: string;
}
export interface BocadillosInv{
    nombre: string;
    estado: string;
    precio: string;
}
export interface CafesInv{
    nombre: string;
    estado: string;
    precio: string;
}
export interface PastasInv{
    nombre: string;
    estado: string;
    precio: string;
}